import { TableCellContextPipe } from './table-cell-context.pipe';

describe('TableCellContextPipe', () => {
  it('create an instance', () => {
    const pipe = new TableCellContextPipe();
    expect(pipe).toBeTruthy();
  });
});
